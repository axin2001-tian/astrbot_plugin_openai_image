# 更新日志

## v1.9.1
- **修复「未能获取已收集的图片」**：收集模式下素材合并会复制候选列表，原代码先合并后落盘，
  导致已落盘的持久路径只进了局部列表、没进收集状态；「开始」时 session 里只剩可能已被清理的
  临时路径（NapCat 等清理 `data/temp` 后即失效）。改为**先落盘、后合并**，确保持久路径成为首选候选
- 已用复现场景验证：旧顺序 session 只剩失效临时路径；新顺序 session 首选为持久路径

## v1.9.0
- **自动撤回状态消息**：收集模式的「已进入收集模式 / 已收集 / 继续收集中」等状态消息，在发送
  「开始」（进入生成）或「取消」（退出）后自动撤回；「正在生成/编辑」进度消息在生成完成、
  结果即将发出前自动撤回，保持聊天整洁
- 新增配置项「自动撤回状态消息」（默认开）；仅在支持撤回的平台（QQ/OneBot）生效，其它平台自动忽略

## v1.8.3
- `/风格列表` 改为**分类菜单**：只发一条菜单消息（11 类 + 数量），回复「数字」或「分类名」
  才发送对应分类的风格词列表（如「5」或「画风」→ 画风类 19 个）
- 菜单 120 秒内有效，仅发起者（或主人）可操作；「取消」退出；不匹配的消息不影响正常聊天

## v1.8.2
- `/风格列表` 改为**按类别分组、每类一条消息**发送：手办类、装扮类、萌化/兽化、风格化、画风、
  材质/实物、周边/设计、摄影/影视、特效/趣味、场景类、视图/参考（共 11 类，169 个风格词）
- 新增分类一致性校验：分类表与风格指令表不一致时记录警告日志，不阻断加载

## v1.8.1
- **风格指令大扩充**：新增 130+ 个风格词（装扮、风格化、萌化/兽化、画风、材质实物、周边设计、
  摄影影视、特效、场景共 9 大类），风格指令总数达 **169 个**
- `/风格列表` 内容较多时分多条发送，避免消息过长

## v1.8.0
- `群分析 图片` 最终方案：**中转站报告长图 + 真实头像名单**——海报由中转站生成（提示词要求只做文字排版、不绘制头像/人脸），
  海报发出后机器人再补发一条**群成员真实头像名单**消息（qlogo 真实头像 + 排行/称号文本，按用户去重）
- 移除本地 PIL 合成长图/头像条的全部代码（不再本地渲染任何图片）
- **修复打包损坏问题**：此前 zip 包内文件被插入空行、metadata 变成 GBK 编码，导致安装报
  `plugin_upload_*` 路径不存在；重新打包（UTF-8、正常行结构），请重新上传安装

## v1.7.9
- `群分析 图片` 模式**统一改为调用中转站生成报告长图**，不再做任何本地 PIL 合成
  （移除「本地合成（真实头像）」与「中转站长图 + 真实头像条」分支，不再本地渲染头像条）
- 配置项「群聊分析图片生成方式」（`report_image_source`）已废弃并隐藏

## v1.7.8
- `群分析 图片` 默认改为**「中转站长图 + 真实头像条」**：海报走中转站生成（提示词要求不绘制假头像），
  另附一行**真实群成员头像**小图（qlogo 真实头像，按发言数排序）
- 「群聊分析图片生成方式」新增三档：**中转站长图 + 真实头像条**（默认）、**本地合成（真实头像）**、
  **中转站（AI 长图，无头像）**

## v1.7.7
- `群分析 图片` 模式默认**本地合成（真实头像）**：下载群成员真实头像嵌入长图，头像完全对应、非 AI 编造
- 新增配置 **「群聊分析图片生成方式」**（`report_image_source`）：可选「本地合成（真实头像）」（默认）或「中转站（AI 长图）」
- 本地合成失败（缺中文字体/渲染失败）自动回退带头像的文字模式；中转站模式提示词要求不绘制头像/人物

## v1.7.6
- `群分析 图片` 模式改回**调用当前中转站的图片接口生成报告长图**（竖版海报）
- 提示词明确要求**只做文字排版、不绘制头像/人脸/人物形象**，避免 AI 编造与群成员不对应的头像
- 中转站生成失败时自动回退文字模式（真实头像消息链）

## v1.7.5
- **修复图片版群分析头像仍为 AI 编造的问题**：`群分析 图片` 本地合成失败时不再回退 AI 生成长图
  （AI 会画与群成员不对应的头像），改为回退**带头像的文字模式**（真实头像消息链）
- 增强中文字体探测：已知路径之外新增 fontconfig（`fc-match :lang=zh`）与常见字体目录按文件名扫描，
  提升 Linux/Docker 等环境下本地长图合成的成功率（减少触发回退）
- **修复热门话题不合理**：`_extract_topics` 改为先剔除 URL/@提及/表情/数字，过滤停用词与单字，
  同一消息内重复词只计一次（避免刷屏词霸榜）；未安装 jieba 时兜底由"整段乱切"改为按标点切词
- `requirements.txt` 新增 `jieba`（中文分词，热门话题主路径依赖）；扩充群聊停用词表

## v1.7.4
- **修复图片版群分析头像不对应的问题**：`群分析 图片` 不再依赖 AI 画头像
- 图片模式改为**本地 PIL 合成报告长图**：下载群成员**真实 QQ 头像**（qlogo，圆形裁剪）嵌入长图，中文排版（龙王榜/称号/话题/总结），生成纯本地、头像完全对应
- 本地合成需要系统中文字体（Windows 微软雅黑 / Linux Noto CJK、文泉驿、文鼎 / macOS 苹方，自动探测）；未找到中文字体或渲染失败时自动回退 AI 生成长图
- 文字模式行为不变（真实头像消息链）

## v1.7.3
- `群分析 补录` 适配**聊天存档插件（astrbot_plugin_chat_archive）**：自动定位其 SQLite 库
  （`data/plugin_data/astrbot_plugin_chat_archive/chat_history.db`，支持配置 db_path 与 ARCHIVE_DB_PATH 环境变量），
  按群号从 `chat_history` 表补录历史消息（文本），去重后并入并持久化；找不到存档库时回退 AstrBot 自带平台消息历史表

## v1.7.2
- 新增 **`/群分析 补录`**：从 AstrBot 数据库（platform_message_history 表）按当前群号翻页拉取**更早的历史消息**，去重后并入记录并持久化（仅管理员/主人）

## v1.7.1
- 群消息记录**持久化到文件**（`data/plugin_data/astrbot_plugin_openai_image/group_msgs/<群号>.jsonl`，JSONL 追加写，每群保留最近 2000 条）
- 插件启动/重载时自动加载历史记录：**重载/重启后旧聊天记录仍在**，`群分析` 可统计旧消息

## v1.7.0
- 群分析**按文档重做**：完全基于[接收消息事件](https://docs.astrbot.app/dev/star/guides/listen-message-event.html)文档的群消息事件监听
- 使用 `@filter.event_message_type(filter.EventMessageType.GROUP_MESSAGE)` 监听，按 `message_obj.group_id` 精确归属当前群，图片数从消息链 `message` 中统计 Image 段，时间用 `message_obj.timestamp`
- **移除 AstrBot 日志解析**（此前因日志文件格式不匹配解析为 0，且日志不含群号）；统计自插件加载起（重启清零），报告明确标注

## v1.6.6
- 日志目录侦察：候选目录扩展到 data/log、data/logs、data/astrbot/log；打印找到的日志文件名与最新文件前几行原始内容（用于排查日志格式不匹配导致解析 0 条的问题）

## v1.6.5
- 日志解析放宽匹配（机器人 ID/平台名格式更宽松、[default] 可选），新增诊断日志：匹配/未匹配条数、未匹配日志行示例、各来源条数与用户分布（排查"只有我的消息"问题）

## v1.6.4
- 群分析数据源**合并**：当前群事件记录（新消息）+ AstrBot 日志解析（旧消息历史），自动去重
- 去重规则：同用户 + 同图片数 + 归一化文本（去掉 [图片]/[At:xxx] 占位符）+ 2 秒内时间差视为同一条
- 报告头部标注两类来源及日志补充条数

## v1.6.3
- 群分析数据源改为**按文档的消息事件方式**采集：独立监听器 `@filter.event_message_type(filter.EventMessageType.GROUP_MESSAGE)` 只接收群消息，准确归属当前群
- 当前群无记录时（如刚重启）回退解析 AstrBot 日志（v1.6.2 保留）；报告标注数据来源

## v1.6.2
- 群分析数据源改为**解析 AstrBot 日志**（data/log/ 下的日志文件，跨重启、含历史；兼容多行/单行两种日志格式），无日志时回退内存记录
- 指令名由「群聊报告」改为 **`群分析`**（原名称保留为别名）

## v1.6.1
- 群聊分析新增**总开关**（配置项「群聊分析开关」，关闭后不再记录消息）与**输出模式**（配置项「群聊分析输出模式」：文字 / 图片）
- **图片模式**：调用当前中转站的图片接口生成报告**长图**（竖版海报，1024x1792），失败自动回退文字模式；指令可临时指定，如「群分析 图片 48」

## v1.6.0
- 新增**群聊分析报告**：`/群聊报告 [小时数]` 输出龙王榜、群友称号、热门话题、总结，榜单条目**带头像**（QQ 平台）；自动记录群消息（内存，上限 2000 条/群，重启清零）

## v1.5.5
- `/风格列表` 恢复显示风格提示词（中文提示词；桌面手办使用中文长提示词）

## v1.5.4
- `/风格列表` 只显示风格名称（不再回显完整提示词）
- `桌面手办` 提示词改为中文（不再出现大段英文）

## v1.5.3
- 新增风格 `桌面手办`：1/7 比例 PVC 手办 + 电脑桌场景（ZBrush 建模屏、BANDAI 风格包装盒、手办制作工具，完整英文提示词）

## v1.5.2
- 移除「桌面手办」风格（后续按需求重新添加为 v1.5.3）

## v1.5.1
- （「桌面手办」风格已移除，未发布使用）

## v1.5.0
- 新增**风格指令**：手办化、Q版化、痛屋化、痛车化、cos化、三视图、真人化 等 40+ 个风格关键词
- 直接发送风格词即可使用（**免 # 前缀**）：带图/引用直接进入收集，纯文字先确认
- 新增 `/风格列表` 指令查看全部风格及说明

## v1.4.10
- 移除提示词预设功能（按需求不需要）

## v1.4.9
- （预设功能已移除，未发布使用）

## v1.4.8
- `/更新日志` 改为展示**从开发初期开始的完整版本历史**（不再只显示最近 5 个版本）

## v1.4.7
- 新增 `/更新日志` 指令：直接查看插件更新日志

## v1.4.6
- 新增 `/设置尺寸` 指令：切换图片尺寸（无参查看当前/可用尺寸，仅管理员/主人）

## v1.4.5
- 图片**收集即落盘**：收集瞬间下载/复制到插件持久目录，避免「开始」时 URL 过期、临时文件被清理导致「未能获取已收集的图片」

## v1.4.4
- `/供应商` 支持**添加/删除**站点：`供应商 添加 <名称> <地址> [Key]`、`供应商 删除 <名称>`；删除当前站点自动切换

## v1.4.3
- 修复 CollectSession 字段顺序错误导致插件无法加载的问题

## v1.4.2
- 「开始」后**生成期间锁定会话**：不再收集聊天消息
- 生成失败后进入**暂停**状态：仅「开始/继续/取消」可唤醒，不再吞聊天内容

## v1.4.1
- 新增**生成任务限流**：默认 60 秒内最多 3 个生成任务，超出自动排队（次数/窗口可配置，0=不限）

## v1.4.0
- 收集模式**归属人**：仅发起者（或主人）可投递素材与控制，他人消息静默忽略
- 免前缀纯文字绘图指令进入收集前需回复「确认」（60 秒有效）；带图/引用直接进入

## v1.3.9
- 免前缀指令收紧：无参数指令（模型、指令列表等）需整句完全匹配，减少聊天误触
- 新增「免前缀指令」开关（默认开）

## v1.3.8
- 修复免前缀收集入口跳过权限校验的问题：未授权用户消息不会触发收集

## v1.3.7
- **群聊免前缀/免 @ 使用指令**：监听器手动解析指令，直接发送 `绘图 xxx`、`白名单 @用户` 即可

## v1.3.6
- 白名单/黑名单昵称过滤：只认纯数字 ID、括号内数字、微信格式 ID；自动清理名单中混入的昵称条目

## v1.3.5
- 白名单/黑名单 At 识别加固（isinstance/类名/type 兼容）、排除 @全体成员、识别日志

## v1.3.4
- 指令文档与提示去除中间空格（紧凑写法，如 `移除@用户`）

## v1.3.3
- 新增 `/白名单`、`/黑名单` 指令：@用户或填 ID 添加、移除/删除、无参查看（仅管理员/主人）

## v1.3.2
- `/供应商` 列表只显示站点昵称

## v1.3.1
- 新增 `/供应商 重命名`；站点改名后「当前站点」自动同步，名称失效自动自愈

## v1.3.0
- **多站点支持**：站点列表（可添加多个中转站）+ 当前站点 + `/供应商` 切换；旧单供应商配置自动迁移

## v1.2.5
- 供应商预设扩充（阿里云百炼/腾讯混元/302.AI），全部自动补全 /v1；收集模式静默吞掉无素材消息

## v1.2.4
- 图片合并去重：共享任一引用的图片视为同一张，不再重复计数

## v1.2.3
- 访问控制规则修正：**设置主人后默认仅主人可用**（白名单可追加放行）

## v1.2.2
- 新增**主人 / 白名单 / 黑名单**访问控制（配置面板设置用户 ID）

## v1.2.1
- 新增 `/指令列表` 指令：全中文展示插件全部指令

## v1.2.0
- 指令全部中文化：主指令 `/绘图`（`/draw`、`/img` 仍兼容）

## v1.1.9
- 全部指令与回复消息补充 emoji（🖼️ ▶️ ➕ ❌ 🔍 ⚙️ 📋 等）

## v1.1.8
- 收集文字**只显示字数**（段数与总字数），不回显内容，保护隐私

## v1.1.7
- 收集确认消息列出已收集文字内容（v1.1.8 按需求改为只显示字数）

## v1.1.6
- QQ/OneBot 图片**多候选解析**（path/url/file 逐个尝试）、带浏览器 UA/Referer 重试下载、OneBot `get_image` API 兜底

## v1.1.5
- 编辑接口不支持（404/405/501 等）自动**回退为仅文字生成**并提示；测试脚本新增编辑接口验证

## v1.1.4
- 生成失败**保留素材**：可补充/修改后直接再次「开始」重试

## v1.1.3
- 收集优先使用**最新图片**作为编辑底图；群聊素材发送提醒；收集日志

## v1.1.2
- 图片发送**自动重试**（微信个人号 CDN 500）；图片与文字分开发送避免重复

## v1.1.1
- 发送前图片归一化：超大图压缩（>4MB）、长边 >2048px 缩放、WebP/GIF/BMP 转码

## v1.1.0
- **收集模式工作流**：`/绘图` 进入收集模式，持续收集图片与文字，「开始」生成、「继续」继续收集、「取消」退出；生成指令名可自定义（配置项「生成指令名」）

## v1.0.5
- **文字和图片收集第二条通道**：消息中直接附带的图片优先于引用消息图片

## v1.0.4
- 图片额度 429 时**自动换模型重试**（最多 4 个候选，全部失败列出失败模型）；新增独立测试脚本 `test_openai_image_api.py`

## v1.0.3
- 报错信息携带实际使用的模型名；针对「No image generation quota」等常见错误给出精准中文提示

## v1.0.2
- **模型按供应商自动获取**（不依赖默认模型）：配置为空或失效时自动从供应商列表选择并保存；模型列表缓存

## v1.0.1
- 常见 API 错误（401/403/404/429/500）自动附带中文排查提示

## v1.0.0
- 初版：对接 OpenAI 图片生成/编辑 API（`/v1/images/generations`、`/v1/images/edits`）
- 供应商预设（OpenAI 官方 / 硅基流动 / 自定义中转站）
- API 地址**自动隐藏并补全 /v1**（无论是否填写都自动识别剥离再补全）
- `/draw` 文生图；引用（回复）含图片的消息自动收集图片与文字进行图片编辑
- `/模型` 获取供应商图像模型列表（与供应商配置分离）；`/设置模型` 切换模型（仅管理员）
- 按 AstrBot v4 官方开发文档编写（`_conf_schema.json` 配置、`metadata.yaml` + `main.py` 结构）
